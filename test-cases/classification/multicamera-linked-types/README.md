# Multicamera classification with linked types

## Contents

Port and starboard each contain the same three logical tracks on eight frames.
`type-hierarchy.config.json` defines the shared taxonomy.

## Data invariant

The camera files have equal track IDs, geometry, attributes, pair order, and confidence values.
This is the non-divergent multicamera baseline.
